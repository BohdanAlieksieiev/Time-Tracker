import React from 'react';
import "./UI.scss";

const EmptyList: React.FC = () => {
    return (
        <section className="empty-list">
            Empty to-do list
        </section>
    )
}

export default EmptyList;
